<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="190dp"
    android:background="@drawable/cubebackground"
    android:orientation="vertical">

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/profile_image"
        android:layout_width="70dp"
        android:layout_height="70dp"
        android:src="@drawable/tessaract"
        android:layout_marginTop="36dp"
        android:layout_alignParentEnd="true"
        android:layout_marginEnd="24dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Time Controller"
        android:textSize="18sp"
        android:textColor="#FFF"
        android:textStyle="bold"
        android:gravity="left"
        android:paddingBottom="6dp"
        android:layout_marginStart="12dp"
        android:id="@+id/username"
        android:layout_above="@+id/credits_blake" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Blake Anderson | Amar Suchak"
        android:id="@+id/credits_blake"
        android:gravity="left"
        android:layout_marginStart="12dp"
        android:layout_marginBottom="12dp"
        android:textSize="14sp"
        android:textColor="@color/MetallicSilver"
        android:layout_alignParentBottom="true" />

</RelativeLayout>
